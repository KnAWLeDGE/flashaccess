make build      # ./bin/flashaccess for your machine
make release    # static linux/amd64 + linux/arm64 in ./dist + checksums.txt
flashaccess version   # -> FlashAccess v0.1.0  (proves ldflags injection)